We can use async and poll for playbooks to play them in background mode and asynchronously, for example in process of downloading for example,
but carefully because we can't call or use files that aren't downloaded.
